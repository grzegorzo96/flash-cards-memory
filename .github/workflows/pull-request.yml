name: Pull Request CI

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests with coverage
        run: npm run test:coverage

      - name: Upload unit test coverage
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: unit-test-coverage
          path: coverage/
          retention-days: 30

  e2e-test:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: lint
    environment: integration
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Create .env.test file
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_PUBLIC_KEY }}
          E2E_USERNAME_ID: ${{ secrets.E2E_USERNAME_ID }}
          E2E_USERNAME: ${{ secrets.E2E_USERNAME }}
          E2E_PASSWORD: ${{ secrets.E2E_PASSWORD }}
        run: |
          printf '%s\n' \
            "SUPABASE_URL=${SUPABASE_URL}" \
            "SUPABASE_KEY=${SUPABASE_KEY}" \
            "E2E_USERNAME_ID=${E2E_USERNAME_ID}" \
            "E2E_USERNAME=${E2E_USERNAME}" \
            "E2E_PASSWORD=${E2E_PASSWORD}" \
            > .env.test

      - name: Verify environment variables
        run: |
          echo "Checking .env.test file..."
          echo "SUPABASE_URL length: $(grep SUPABASE_URL .env.test | cut -d= -f2 | wc -c)"
          echo "SUPABASE_KEY length: $(grep SUPABASE_KEY .env.test | cut -d= -f2 | wc -c)"
          echo "E2E_USERNAME_ID length: $(grep E2E_USERNAME_ID .env.test | cut -d= -f2 | wc -c)"
          echo "E2E_USERNAME: $(grep E2E_USERNAME .env.test | cut -d= -f2)"
          echo "E2E_PASSWORD length: $(grep E2E_PASSWORD .env.test | cut -d= -f2 | wc -c)"

      - name: Run E2E tests
        run: npm run test:e2e

      - name: Upload debug screenshots
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: debug-screenshots
          path: |
            before-login.png
            after-login-failed.png
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Playwright report
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  status-comment:
    name: PR Status Comment
    runs-on: ubuntu-latest
    needs: [lint, unit-test, e2e-test]
    if: always() && needs.lint.result == 'success' && needs.unit-test.result == 'success' && needs.e2e-test.result == 'success'
    permissions:
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download unit test coverage
        uses: actions/download-artifact@v7
        with:
          name: unit-test-coverage
          path: coverage-unit/

      - name: Create PR comment
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = require('path');
            
            let comment = '## ✅ Pull Request CI Status\n\n';
            comment += '### All checks passed successfully!\n\n';
            comment += '| Job | Status |\n';
            comment += '|-----|--------|\n';
            comment += '| Lint | ✅ Passed |\n';
            comment += '| Unit Tests | ✅ Passed |\n';
            comment += '| E2E Tests | ✅ Passed |\n\n';
            
            // Add coverage summary if available
            const unitCoveragePath = 'coverage-unit/coverage-summary.json';
            if (fs.existsSync(unitCoveragePath)) {
              const unitCoverage = JSON.parse(fs.readFileSync(unitCoveragePath, 'utf8'));
              const total = unitCoverage.total;
              comment += '### Unit Test Coverage\n\n';
              comment += '| Type | Coverage |\n';
              comment += '|------|----------|\n';
              comment += `| Lines | ${total.lines.pct}% |\n`;
              comment += `| Statements | ${total.statements.pct}% |\n`;
              comment += `| Functions | ${total.functions.pct}% |\n`;
              comment += `| Branches | ${total.branches.pct}% |\n\n`;
            }
            
            comment += '---\n';
            comment += `*Updated: ${new Date().toISOString()}*`;
            
            // Post comment
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
