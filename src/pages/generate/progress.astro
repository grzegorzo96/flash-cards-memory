---
import AppLayout from "@/layouts/AppLayout.astro";
import GenerateProgressPage from "@/components/generate/GenerateProgressPage";

const url = new URL(Astro.request.url);
const requestId = url.searchParams.get("requestId");
---

<AppLayout title="Generuj fiszki - W toku - FlashCardMemory" user={Astro.locals.user}>
  <GenerateProgressPage requestId={requestId} client:load />
</AppLayout>
